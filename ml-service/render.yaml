services:
  - type: web
    name: equalvoice-api
    env: python
    startCommand: uvicorn ml-service.main:app --host 0.0.0.0 --port 10000
    plan: free
    envVars:
      - key: PORT
        value: 10000
